# 工作生活专属 APP — 产品需求文档（PRD）

- 版本：V1.0（第一版）
- 日期：2026-08-30
- 状态：已确认，作为后续开发依据
- 使用方式：仅本人使用，本地浏览器运行，不上网、不登录、不部署、无手机端

---

## 1. 产品目标和使用场景

### 产品目标
做一个只供本人使用的工作生活管理 App，把工作（自媒体、开发、咨询）和生活（健身、饮食、游戏娱乐）集中在同一个本地工具里管理，打开就能看到今天要做什么、每个模块进展如何。

### 使用场景
- 在个人电脑的浏览器中打开使用（通过双击启动器启动）。
- 不需要登录、不需要联网、不需要云数据库、不需要部署、不需要手机端。
- 数据保存在当前电脑的文件夹里，刷新、关闭、重启后数据不丢失。
- 单人使用，无多用户概念。

---

## 2. 整体页面框架和导航结构

### 布局
- 左侧固定侧边栏：9 个模块导航入口（图标 + 文字），底部显示数据保存状态和当前日期。
- 顶部栏：当前日期、浅色/深色主题切换。
- 主内容区：根据侧边栏选择显示对应模块页面。

### 导航结构（9 个模块）
1. 首页总览
2. 今日计划
3. 自媒体
4. 开发工作
5. 咨询工作
6. 健身计划
7. 饮食计划
8. 游戏娱乐
9. 数据与设置

### 通用交互约定
- 添加：每个页面有「+ 添加」按钮，弹窗表单填写。
- 编辑：悬停条目出现铅笔图标，点击弹窗修改。
- 删除：删除前弹轻量确认，避免误删。
- 完成：可勾选的事项用复选框/打勾。
- 空状态：模块第一次使用时显示引导语，不显示空表格。
- 日期：统一的日期切换条（← 今天 →），可快速回到当天。
- 自动保存：每次操作实时写入数据文件，顶部显示「已保存」。

---

## 3. 首页的内容结构
- 顶部：问候语 + 今天日期。
- 「今日计划」卡片：当天完成进度条 + 今日事项列表，点「去安排」跳转今日计划。
- 「快速备忘」卡片：输入框随手记录，列表可勾选完成、可删除。
- 模块摘要网格（3 列卡片）：每个模块一张卡，显示最关键 1~2 条信息，点击跳转对应模块。

---

## 4. 每个模块的用途
- 首页总览：一站看清今天和全局，作为所有模块的聚合入口。
- 今日计划：管理每天要做的事，按上午/下午/晚上安排。
- 自媒体：管理内容创作全流程（选题 → 撰写 → 待发布 → 已发布）及平台、排期、灵感和发布数据。
- 开发工作：管理项目、项目下的任务和按日期的开发日志。
- 咨询工作：管理客户档案、预约安排、咨询记录和收入流水。
- 健身计划：管理训练模板、每日训练打卡、训练历史和身体数据。
- 饮食计划：记录每天三餐和喝水情况，并保存固定饮食模板。
- 游戏娱乐：管理游戏库、游戏时间记录和心愿单。
- 数据与设置：管理数据备份恢复和个性化设置。

---

## 5. 每个模块第一版最必要的数据和操作

### 首页总览
- 数据：今日计划摘要、快速备忘列表、各模块摘要。
- 操作：添加/完成/删除备忘；点击摘要卡片跳转对应模块。

### 今日计划
- 数据：事项（内容、日期、时间段[上午/下午/晚上]、重要程度[普通/重要]、备注、完成状态）。
- 操作：添加/编辑/删除；勾选完成；按日期切换；查看当天完成率；一键把未完成复制到明天。

### 自媒体
- 数据：
  - 平台（名称、账号、备注）
  - 内容创作（标题/内容、所属平台、状态[构思中/撰写中/待发布/已发布]、排期日期）
  - 灵感（内容、来源、日期）
  - 发布数据（阅读、点赞、评论）
- 操作：平台增删改；内容增删改与状态流转；查看发布排期；灵感增删；发布数据记录。

### 开发工作
- 数据：
  - 项目（名称、技术栈、状态[进行中/暂停/完成]、备注）
  - 任务（所属项目、内容、状态[待办/进行中/已完成]、优先级）
  - 开发日志（所属项目、日期、内容）
- 操作：项目增删改；进入项目详情；任务增删改与状态切换；日志按日期追加。

### 咨询工作
- 数据：
  - 客户（称呼、联系方式、来源、备注）
  - 预约（日期时间、客户、主题）
  - 咨询记录（日期、客户、主题、时长、要点）
  - 收入（日期、客户、金额、状态[未收/已收]）
- 操作：客户增删改与详情查看；预约增删改；咨询记录增删改；收入增删改与收款状态切换。

### 健身计划
- 数据：
  - 训练模板（名称、动作列表[动作名、组数/次数]）
  - 训练打卡（日期、所用模板、每个动作的重量/次数/是否完成）
  - 身体数据（日期、体重）
  - 周目标（每周训练次数）
- 操作：模板增删改；今日训练选择模板并逐动作打卡；查看训练历史；记录体重；设置周目标。

### 饮食计划
- 数据：
  - 每日饮食（日期、早餐/午餐/晚餐/加餐内容、喝水记录）
  - 饮食模板（名称、早/午/晚内容）
- 操作：每日三餐打卡记录；喝水打卡；模板增删改；按日期回顾历史。

### 游戏娱乐
- 数据：
  - 游戏库（名称、状态[想玩/在玩/通关/弃坑]、备注）
  - 游戏时间记录（日期、游戏、时长、备注）
  - 心愿单（游戏名、价格、优先级、是否已买）
- 操作：游戏库增删改与状态切换；时间记录增删改并按日期汇总；心愿单增删改与已买标记。

### 数据与设置
- 数据：数据文件路径；设置项（主题、每周起始日、首页显示哪些模块摘要）。
- 操作：一键导出备份；一键导入恢复；主题切换；设置修改。

---

## 6. 首页、今日计划和各模块之间的关系
- 首页是聚合入口：展示今日计划摘要、快速备忘、各模块摘要，点击可跳转到对应模块。
- 今日计划是独立模块，管理每天事项；首页的今日计划卡片是它的摘要，可跳转回今日计划。
- 各模块是独立数据域；首页摘要只读展示模块中最关键的信息（如今天要发什么、本周练几次、有没有未收款），不在首页修改模块数据。
- 所有模块数据统一存储在同一份本地数据文件中，模块之间通过「日期」和「摘要」关联。

---

## 7. 本地数据文件、备份和恢复要求
- 所有数据保存在 App 文件夹内的 `data.json` 文件中，肉眼可见、可手动复制备份。
- 每次操作实时自动保存；先写临时文件再替换，避免写坏。
- 自动保留一个最近的备份文件（如 `data.backup.json`），数据文件损坏时可自动恢复。
- 「数据与设置」页提供：一键导出全部数据为文件、一键从备份文件导入恢复。
- 数据文件路径在「数据与设置」页可见。

---

## 8. 第一版必须完成的功能
1. 双击启动器打开本地小服务，并在浏览器自动打开 App。
2. 侧边栏 9 个模块导航，可切换页面。
3. 首页：今日计划摘要、快速备忘、模块摘要卡片及跳转。
4. 今日计划：事项增删改、勾选完成、按日期切换、分组显示、完成率、未完成复制到明天。
5. 自媒体：平台管理、内容创作状态流、发布排期、灵感库、发布数据记录。
6. 开发工作：项目管理、任务管理、开发日志。
7. 咨询工作：客户、预约、咨询记录、收入流水。
8. 健身计划：训练模板、今日打卡、训练历史、体重记录、周目标。
9. 饮食计划：每日三餐与喝水打卡、饮食模板、历史回顾。
10. 游戏娱乐：游戏库、时间记录、心愿单。
11. 数据与设置：自动保存、数据文件路径可见、导出/导入、主题切换、设置项。
12. 所有数据在刷新/关闭/重启后不丢失。

---

## 9. 第一版暂时不做的功能
- 手机版 / 平板适配（只做电脑浏览器）。
- 云同步、多设备、联网、登录。
- 系统级弹窗提醒和声音提醒（只做 App 内提示）。
- 复杂图表、统计报表（只做简单数字摘要）。
- 上传图片、附件、文件。
- 账号体系、多人协作。
- 复杂自动化（如定时自动生成计划）。
- 各模块深度专业功能（如对接自媒体平台 API、海量健身动作库等）。

---

## 10. 验收标准（可实际检查）
1. 启动：双击启动器后，浏览器自动打开 App，无需手动输入命令、无需联网。
2. 导航：侧边栏 9 个模块均可点击切换，页面内容正确显示。
3. 持久化：在任意模块添加数据后，刷新浏览器、关闭浏览器、重启电脑后再次打开，数据仍在。
4. 数据文件：App 文件夹内存在 `data.json`；「数据与设置」页能看到数据文件路径。
5. 备份恢复：可一键导出备份文件；删除部分数据后导入备份可恢复；`data.json` 损坏时可由备份自动恢复。
6. 首页：显示今日计划摘要、快速备忘、各模块摘要卡片；点击摘要可跳转到对应模块。
7. 今日计划：可添加/编辑/删除/勾选事项；可按日期切换；可将未完成事项复制到明天。
8. 自媒体：可新增平台、创建内容并流转状态（构思中 → 撰写中 → 待发布 → 已发布）、记录发布数据、记录灵感。
9. 开发工作：可新建项目，在项目下新建任务并切换状态，按日期写开发日志。
10. 咨询工作：可新增客户、预约、咨询记录；收入可标记未收/已收。
11. 健身计划：可新建训练模板、按模板完成今日打卡、查看历史、记录体重、设置周目标。
12. 饮食计划：可记录早/午/晚/加餐和喝水、保存饮食模板、按日期回顾。
13. 游戏娱乐：可管理游戏库状态、记录游戏时长并按日期汇总、维护心愿单。
14. 设置：可切换浅色/深色主题；可修改每周起始日和首页摘要模块勾选，刷新后设置保持。
15. 离线可用：断网状态下所有功能正常。
16. 无登录：打开 App 不出现任何登录/注册入口。